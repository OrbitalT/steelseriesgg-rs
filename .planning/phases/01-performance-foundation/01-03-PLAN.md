---
phase: 01-performance-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified: [src/devices/discovery.rs, src/devices/hid_reports.rs, src/main.rs]
autonomous: true

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "System detects USB device insertion and removal in real-time"
    - "Rapid connect/disconnect sequences are handled without crashes"
    - "Device discovery automatically recovers from USB errors"
    - "Hot-plug events integrate seamlessly with daemon operation"
  artifacts:
    - path: "src/devices/hid_reports.rs"
      provides: "Enhanced HID error handling and recovery mechanisms"
      min_lines: 50
      exports: ["HidErrorRecovery", "ReportValidator"]
    - path: "src/devices/discovery.rs"
      provides: "USB hot-plug monitoring with event-driven device management"
      contains: "hotplug_monitor"
    - path: "src/main.rs"
      provides: "Integrated hot-plug handling in daemon loop"
      contains: "hotplug_handler"
  key_links:
    - from: "src/devices/discovery.rs"
      to: "USB monitoring"
      via: "udev event subscription or polling"
      pattern: "monitor_usb_events|poll_devices"
    - from: "src/devices/hid_reports.rs"
      to: "error recovery"
      via: "automatic retry and device reset logic"
      pattern: "recover_from_error|retry_operation"
    - from: "src/main.rs"
      to: "hot-plug integration"
      via: "device event handling in daemon"
      pattern: "handle_device_event"
---

<objective>
Implement robust device hot-plug support with graceful handling of rapid connect/disconnect scenarios.

Purpose: Provide seamless device management that automatically handles USB hot-plug events without requiring daemon restart, improving user experience and system reliability.

Output: Production-ready hot-plug device management with automatic error recovery and seamless integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-performance-foundation/01-02-SUMMARY.md
@src/devices/discovery.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HID error handling and recovery system</name>
  <files>src/devices/hid_reports.rs</files>
  <action>
    Create robust error handling system for HID communication failures during hot-plug scenarios.

    Implement:
    - HidErrorRecovery struct with exponential backoff retry logic
    - ReportValidator that checks HID report integrity before transmission
    - recover_from_error() that attempts device reset and reconnection
    - Error categorization (transient vs permanent) for appropriate response
    - Connection health monitoring to proactively detect failing devices

    Handle USB disconnect gracefully without crashing daemon. Use hidapi error codes to determine if errors are recoverable. Include circuit breaker pattern to prevent infinite retry loops.
  </action>
  <verify>cargo check succeeds, error handling compiles</verify>
  <done>HID error handling system provides robust recovery from USB communication failures</done>
</task>

<task type="auto">
  <name>Task 2: Enhance device discovery with hot-plug monitoring</name>
  <files>src/devices/discovery.rs</files>
  <action>
    Extend DeviceManager to support real-time USB hot-plug event monitoring and device lifecycle management.

    Enhance DeviceManager:
    - Add hotplug_monitor() method using periodic device enumeration (fallback approach for cross-platform)
    - Implement device_added/device_removed event callbacks
    - Add device fingerprinting to track devices across reconnections
    - Include rate limiting to handle rapid connect/disconnect without performance impact
    - Maintain device registry for consistent device identification

    Use existing hidapi enumerate() with change detection rather than platform-specific udev to maintain portability. Poll every 2 seconds for device changes.
  </action>
  <verify>cargo build succeeds, device discovery compiles</verify>
  <done>Device discovery supports hot-plug monitoring with automatic device lifecycle management</done>
</task>

<task type="auto">
  <name>Task 3: Integrate hot-plug handling into daemon loop</name>
  <files>src/main.rs</files>
  <action>
    Integrate hot-plug device management into the daemon's main event loop with seamless device state transitions.

    Modify cmd_daemon() to include:
    - Spawn hotplug monitoring task alongside animation loop
    - Add device event handler that updates daemon state when devices connect/disconnect
    - Implement graceful device removal (clean up RGB controllers, save final state)
    - Add device addition logic (initialize RGB controller, apply saved profiles)
    - Include hot-plug event logging for debugging and monitoring

    Ensure hot-plug events don't interfere with existing RGB animations. Use the existing daemon_state Arc<RwLock> for thread-safe device management. Handle concurrent access carefully.
  </action>
  <verify>cargo build --release succeeds, daemon compiles</verify>
  <done>Daemon loop integrates hot-plug handling with seamless device state management and profile application</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test` passes all tests
- [ ] `cargo build --release` succeeds
- [ ] Hot-plug monitoring compiles and integrates with daemon
- [ ] Error recovery system handles HID communication failures
- [ ] Device lifecycle management maintains consistent state
</verification>

<success_criteria>
- All tasks completed successfully
- Hot-plug monitoring detects device changes in real-time
- Error recovery system handles USB communication failures gracefully
- Daemon seamlessly manages device lifecycle without manual intervention
- Rapid connect/disconnect scenarios are handled without crashes or state corruption
</success_criteria>

<output>
After completion, create `.planning/phases/01-performance-foundation/01-03-SUMMARY.md`
</output>