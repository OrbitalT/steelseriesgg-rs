---
phase: 01-performance-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main.rs, src/rgb/mod.rs, src/performance.rs]
autonomous: true

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "RGB effects update at consistent sub-16ms intervals under system load"
    - "Multiple concurrent RGB effects render smoothly without frame drops"
    - "Animation timing adapts to effect complexity and device capabilities"
    - "Performance metrics can be monitored and reported in real-time"
  artifacts:
    - path: "src/performance.rs"
      provides: "Performance monitoring and metrics collection"
      min_lines: 80
      exports: ["PerformanceMonitor", "RgbTimingMetrics"]
    - path: "src/rgb/mod.rs"
      provides: "Optimized RGB effect rendering with adaptive timing"
      contains: "adaptive_timing"
    - path: "src/main.rs"
      provides: "Enhanced daemon loop with performance monitoring"
      contains: "PerformanceMonitor"
  key_links:
    - from: "src/main.rs"
      to: "PerformanceMonitor"
      via: "performance tracking in daemon loop"
      pattern: "PerformanceMonitor::"
    - from: "src/rgb/mod.rs"
      to: "adaptive timing logic"
      via: "effect complexity calculation"
      pattern: "calculate_optimal_timing"
---

<objective>
Implement adaptive RGB timing engine with performance monitoring to achieve consistent sub-16ms updates.

Purpose: Replace the conservative 50ms fixed timing with intelligent adaptive timing that optimizes frame rate based on effect complexity and system capabilities while maintaining smooth animation under load.

Output: High-performance RGB rendering system with real-time performance metrics and adaptive timing optimization.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@src/main.rs
@src/rgb/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create performance monitoring module</name>
  <files>src/performance.rs</files>
  <action>
    Create a comprehensive performance monitoring system that tracks RGB frame timing, memory usage, and CPU utilization.

    Implement:
    - RgbTimingMetrics struct with frame_time, target_fps, actual_fps, dropped_frames
    - PerformanceMonitor struct with ring buffer for timing history
    - calculate_optimal_timing() function that analyzes effect complexity and system load
    - Memory usage tracking for daemon stability monitoring

    Use std::time::Instant for high-precision timing measurements. Include exponential moving average for smooth metric reporting.
  </action>
  <verify>cargo check succeeds, rustdoc builds documentation</verify>
  <done>Performance monitoring module provides real-time RGB timing metrics and adaptive timing calculations</done>
</task>

<task type="auto">
  <name>Task 2: Implement adaptive RGB timing in effect engine</name>
  <files>src/rgb/mod.rs</files>
  <action>
    Enhance the EffectEngine to support adaptive timing based on effect complexity and performance metrics.

    Modify existing EffectEngine:
    - Add timing_mode field (Fixed, Adaptive, HighPerformance)
    - Implement calculate_frame_budget() based on effect type (Static=33ms, Breathing=16ms, Spectrum=8ms, Wave=12ms)
    - Add effect complexity scoring (simple effects can run slower, complex effects need consistent timing)
    - Optimize color computation caching to reduce CPU load for repeated calculations

    Maintain backward compatibility with existing effect types. Use the existing cache_threshold but make it adaptive instead of fixed.
  </action>
  <verify>cargo test passes, RGB effects compile without warnings</verify>
  <done>RGB effects support adaptive timing with complexity-based frame budgets</done>
</task>

<task type="auto">
  <name>Task 3: Optimize daemon animation loop with adaptive timing</name>
  <files>src/main.rs</files>
  <action>
    Replace the fixed 50ms interval in cmd_daemon() with adaptive timing system using PerformanceMonitor.

    In the animation_task (around line 1970):
    - Initialize PerformanceMonitor and track frame timing
    - Calculate optimal interval based on active effects and system performance
    - Use tokio::time::interval with dynamic duration updates
    - Add frame drop detection and recovery logic
    - Implement graceful degradation when system is under load (increase interval temporarily)

    Target: Achieve consistent sub-16ms timing for smooth effects while preventing USB bus saturation. Keep existing MissedTickBehavior::Skip for robustness.
  </action>
  <verify>cargo build succeeds, daemon mode compiles without errors</verify>
  <done>Daemon loop uses adaptive timing with performance monitoring and graceful degradation under load</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test` passes all tests
- [ ] `cargo build --release` succeeds without warnings
- [ ] Performance monitoring compiles and provides metrics
- [ ] RGB effects support adaptive timing configuration
- [ ] Daemon loop implements adaptive timing with frame drop recovery
</verification>

<success_criteria>
- All tasks completed successfully
- RGB animation timing is adaptive and performance-aware
- Performance monitoring system provides real-time metrics
- Frame timing consistently achieves sub-16ms targets for complex effects
- System gracefully handles performance degradation under load
</success_criteria>

<output>
After completion, create `.planning/phases/01-performance-foundation/01-01-SUMMARY.md`
</output>