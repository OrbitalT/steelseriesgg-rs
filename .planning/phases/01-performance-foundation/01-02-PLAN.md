---
phase: 01-performance-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/device_state.rs, src/devices/mod.rs, src/validation.rs]
autonomous: true

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "HID communication CPU usage is reduced by 20% from baseline"
    - "Memory usage remains stable during extended daemon operation (24+ hours)"
    - "Device state persistence does not block RGB updates"
    - "System validates and reports resource usage metrics"
  artifacts:
    - path: "src/validation.rs"
      provides: "Resource usage validation and monitoring"
      min_lines: 60
      exports: ["ResourceValidator", "MemoryTracker"]
    - path: "src/device_state.rs"
      provides: "Async device state persistence with write-behind caching"
      contains: "async fn save"
    - path: "src/devices/mod.rs"
      provides: "Optimized HID communication patterns"
      contains: "optimized_write"
  key_links:
    - from: "src/device_state.rs"
      to: "async persistence"
      via: "non-blocking writes with batching"
      pattern: "tokio::spawn.*save"
    - from: "src/validation.rs"
      to: "memory tracking"
      via: "periodic memory usage collection"
      pattern: "track_memory_usage"
    - from: "src/devices/mod.rs"
      to: "HID optimization"
      via: "reduced system calls and batched operations"
      pattern: "batch_write|optimized_write"
---

<objective>
Optimize system resource usage to achieve 20% CPU reduction and stable memory consumption.

Purpose: Eliminate synchronous I/O bottlenecks and optimize HID communication patterns to improve daemon efficiency and reliability during extended operation.

Output: Resource-efficient device communication and state management with comprehensive monitoring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@src/device_state.rs
@src/devices/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement async device state persistence</name>
  <files>src/device_state.rs</files>
  <action>
    Replace synchronous file I/O with async write-behind caching system to prevent blocking RGB updates.

    Enhance DeviceStateStore:
    - Convert save operations to async with tokio::fs for non-blocking I/O
    - Implement write-behind cache with 5-second flush interval for batching
    - Add dirty flag tracking to avoid unnecessary writes
    - Include atomic writes using temp files to prevent corruption
    - Maintain backward compatibility for existing sync API by using tokio::spawn for background writes

    Keep the current FNV hash optimization but improve the overall I/O pattern. Add error logging for failed async writes without blocking callers.
  </action>
  <verify>cargo test passes, async operations don't block</verify>
  <done>Device state persistence is fully async with write-behind caching and atomic updates</done>
</task>

<task type="auto">
  <name>Task 2: Create resource validation and monitoring system</name>
  <files>src/validation.rs</files>
  <action>
    Build comprehensive resource monitoring system to track memory usage and validate system performance.

    Implement:
    - ResourceValidator struct with memory tracking and leak detection
    - MemoryTracker that samples RSS, heap usage, and file descriptor counts
    - validate_memory_stability() that detects memory leaks using sliding window analysis
    - CPU usage sampling for HID communication operations
    - Baseline measurement system for comparing before/after optimization

    Use procfs or /proc/self/status for accurate memory metrics on Linux. Include warning thresholds for resource usage alerts.
  </action>
  <verify>cargo check succeeds, validation module compiles</verify>
  <done>Resource validation system provides accurate memory and CPU usage monitoring with leak detection</done>
</task>

<task type="auto">
  <name>Task 3: Optimize HID communication for 20% CPU reduction</name>
  <files>src/devices/mod.rs</files>
  <action>
    Optimize HID communication patterns to reduce CPU usage through batching and syscall reduction.

    Enhance Device trait implementation:
    - Add optimized_write() method that batches multiple RGB zone updates into single HID report
    - Implement report caching to avoid duplicate writes with same data
    - Reduce error handling overhead by caching device connectivity status
    - Add write coalescing for rapid successive updates (common in animations)
    - Optimize mutex lock duration by preparing reports outside critical sections

    Target 20% CPU reduction from current baseline. Keep existing error handling but make it more efficient. Maintain compatibility with existing send_raw() interface.
  </action>
  <verify>cargo build succeeds, HID operations compile without warnings</verify>
  <done>HID communication is optimized with batching, caching, and reduced syscall overhead</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test` passes all existing tests
- [ ] `cargo build --release` succeeds
- [ ] Async operations don't block main thread
- [ ] Resource monitoring provides accurate metrics
- [ ] HID optimizations maintain device compatibility
</verification>

<success_criteria>
- All tasks completed successfully
- Device state persistence is fully async and non-blocking
- Resource monitoring system tracks memory and CPU usage
- HID communication patterns are optimized for reduced CPU usage
- Memory usage remains stable under extended operation testing
</success_criteria>

<output>
After completion, create `.planning/phases/01-performance-foundation/01-02-SUMMARY.md`
</output>